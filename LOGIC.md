# Логика системы Nirvana Luxury Residence — Склад и Снабжение

---

## 🏗 АРХИТЕКТУРА

```
Firebase Firestore (realtime)
├── /requests      — заявки (основная коллекция)
├── /objects       — объекты строительства
├── /users         — пользователи
├── /stock         — остатки склада
├── /movements     — движения склада
└── /settings/telegram — конфиг Telegram-бота

React + TypeScript + Vite → Firebase Hosting
GitHub Actions → auto-deploy при push в main
URL: https://sklad-25dbd.web.app
```

---

## 👥 РОЛИ И ДОСТУПЫ

| Роль | Видит заявки | Навигация |
|---|---|---|
| `prоrab` | Только свои | Дашборд, Заявки, Склад, Объекты |
| `sklad` | Все | + Склад (полный доступ) |
| `nachalnik` | Все | + Аналитика |
| `finansist` | Все | + Аналитика |
| `snab` | Все | + Аналитика |
| `admin` | Все | + Пользователи, Telegram |

---

## 📋 ТИПЫ ЗАЯВОК → ЦЕПОЧКИ (автоматически)

```
materials  → full           (Склад → Нач. → Снаб.)
tools      → warehouse_only (Только склад)
equipment  → full_finance   (Склад → Нач. → Финансист → Снаб.)
services   → finance_only   (Нач. → Финансист → Снаб.)
other      → full
```

Цепочку можно переопределить вручную при создании заявки.

---

## 🔄 ВОРОНКА СТАТУСОВ

```
novaya
  │
  ├─[warehouse_only]────────────────────────────────────────► vydano / otkloneno
  │
  ├─[full / full_finance]──► sklad_review ─────────────────► vydano
  │                               └──► sklad_partial ──────► nachalnik_review
  │
  ├─[purchase_only]────────────────────────────────────────► nachalnik_review
  └─[finance_only]─────────────────────────────────────────► finansist_review

nachalnik_review
  ├─[full / purchase_only]──────────────────────────────────► nachalnik_approved
  ├─[full_finance / finance_only]───────────────────────────► finansist_review
  └─────────────────────────────────────────────────────────► otkloneno

nachalnik_approved
  ├─[full / purchase_only]──────────────────────────────────► snab_process
  ├─[warehouse_only / full_finance]─────────────────────────► vydano / sklad_partial (склад выдаёт)
  └─────────────────────────────────────────────────────────► otkloneno

finansist_review ──► finansist_approved ──► snab_process
                └──► otkloneno

snab_process ──► zakupleno ──► vydano (склад подтверждает получение)
```

### Ответственный по статусу (`getResponsibleRole`)

```
novaya              → sklad  (или nachalnik для purchase_only / finance_only)
sklad_review        → sklad
sklad_partial       → nachalnik
nachalnik_review    → nachalnik
nachalnik_approved  → snab   (или sklad для warehouse_only / full_finance)
finansist_review    → finansist
finansist_approved  → snab
snab_process        → snab
zakupleno           → sklad
vydano / otkloneno  → никто (финальные)
```

### Доступные переходы по роли (`getNextStatuses`)

| Роль | Статус | Может перевести в |
|---|---|---|
| sklad | novaya (full) | vydano, sklad_partial, nachalnik_review |
| sklad | novaya (warehouse_only) | vydano, sklad_partial |
| sklad | sklad_partial | vydano, nachalnik_review |
| sklad | nakhalnik_approved / finansist_approved | vydano, sklad_partial |
| sklad | zakupleno | vydano |
| nachalnik | nachalnik_review (full) | nachalnik_approved, otkloneno |
| nachalnik | nachalnik_review (full_finance) | finansist_review, otkloneno |
| finansist | finansist_review | finansist_approved, otkloneno |
| snab | nachalnik_approved | snab_process, otkloneno |
| snab | snab_process | zakupleno |
| prоrab | novaya | otkloneno (только свою) |
| admin | любой | любой другой |

---

## 📦 МОДЕЛЬ ЗАЯВКИ (SkladRequest)

### Обязательные поля

```typescript
number          : number         // автоинкремент
title           : string         // название заявки
objectName      : string         // название объекта
objectId?       : string         // ID объекта из /objects
createdBy       : string         // UID инициатора
createdByName   : string         // имя инициатора
createdAt       : string         // ISO дата создания
updatedAt       : string         // ISO дата последнего изменения
status          : RequestStatus  // текущий этап воронки
chain           : RequestChain   // цепочка согласования
requestType     : RequestType    // тип заявки
urgencyLevel    : UrgencyLevel   // low / normal / high / critical
priority        : 'normal' | 'urgent'
items[]         : RequestItem[]  // позиции спецификации
history[]       : RequestHistoryEntry[]  // полная история переходов
```

### Позиция спецификации (RequestItem)

```typescript
id              : string
name            : string         // название материала
unit            : string         // ед. изм.
quantity        : number         // количество
issuedQty?      : number         // выдано со склада
purchasedQty?   : number         // закуплено
estimatedPrice? : number         // цена за единицу (ориентировочная)
actualPrice?    : number         // фактическая цена
category?       : string         // категория материала
```

### История изменений (RequestHistoryEntry)

```typescript
at         : string   // ISO момент события
by         : string   // UID исполнителя
byName     : string   // имя исполнителя
action     : string   // текстовое описание
fromStatus?: RequestStatus
toStatus?  : RequestStatus
comment?   : string   // комментарий при переходе
```

### Дополнительные поля заявки

```typescript
plannedDate?        : string   // желаемая дата получения (YYYY-MM-DD)
deliveryAddress?    : string   // куда доставить или где выдать
zone?               : string   // участок/блок объекта («Секция А», «Этаж 3»)
estimatedCost?      : number   // смета (авторасчёт из позиций × цены)
actualCost?         : number   // фактическая стоимость
budgetCode?         : string   // код статьи бюджета
preferredSupplier?  : string   // предпочтительный поставщик
subcontractors?     : string[] // субподрядчики
responsibleUid?     : string   // UID получателя материала
responsibleName?    : string   // имя получателя
attachments?        : string[] // URL файлов (Firebase Storage)
tags?               : string[] // теги для группировки закупок
slaEnteredAt?       : string   // ISO — когда вошли в текущий статус

// Комментарии по этапам
commentProrab?      : string
commentSklad?       : string
commentNachalnik?   : string
commentFinansist?   : string
commentSnab?        : string

// Кто обработал на каждом этапе
skladProcessedBy?         : string
skladProcessedByName?     : string
nachalnikProcessedBy?     : string
nachalnikProcessedByName? : string
finansistProcessedBy?     : string
finansistProcessedByName? : string
snabProcessedBy?          : string
snabProcessedByName?      : string
```

---

## ⏱ SLA ТАЙМЕРЫ

Время рассчитывается от `slaEnteredAt` (или `updatedAt`). При переводе статуса `slaEnteredAt` сбрасывается.

| Статус | Лимит | Смысл |
|---|---|---|
| novaya | 48 ч | Склад должен принять заявку |
| sklad_review | 24 ч | Склад проверяет наличие |
| sklad_partial | 12 ч | Отделить что выдать / что в закуп |
| nachalnik_review | 24 ч | Начальник одобряет |
| finansist_review | **24 ч** | Финансист согласует бюджет (жёсткий) |
| snab_process | 72 ч | Снабжение размещает заказ |
| zakupleno | 48 ч | Привезти и выдать со склада |

**Визуализация:**
- ≤ 75% времени → серый «SLA: ещё ~Nч»
- 75–100% → **оранжевое** предупреждение
- > 100% → **красный** ring + «⚠ SLA просрочен на Nч»

---

## 🗂 КАНБАН-ДОСКА

### 6 колонок по воронке

| # | Колонка | Цвет | Статусы внутри | WIP лимит |
|---|---|---|---|---|
| 1 | Новые | Серый `#6b7280` | `novaya` | 10 |
| 2 | У склада | Жёлтый `#d97706` | `sklad_review`, `sklad_partial` | 8 |
| 3 | Согласование | Синий `#2563eb` | `nachalnik_review`, `nachalnik_approved` | 12 |
| 4 | Финансы | Фиолетовый `#7c3aed` | `finansist_review`, `finansist_approved` | 8 |
| 5 | Закупка | Голубой `#0891b2` | `snab_process`, `zakupleno` | 15 |
| 6 | Завершено | Зелёный `#15803d` | `vydano`, `otkloneno` | ∞ |

### Возможности Канбана

- **Канбан — вид по умолчанию** (список — второй)
- **Inline-действия** — кнопка «Обработать» меняет статус прямо с карточки без открытия заявки
- **Цепочка с таймингом** — разворачивается прямо в карточке; показывает пройденные этапы с количеством дней на каждом
- **SLA-бейдж** на каждой карточке
- **Теги материалов** цветными маркерами
- **Сортировка** карточек: срочность / дата / сумма / обновление
- **Группировка по объектам** (swimlane — строки × колонки)
- **Свернуть колонку** до 48px полоски, развернуть обратно
- **WIP-индикатор** — прогресс-бар + красный бейдж при превышении лимита
- **Компактный режим** карточек
- **Поиск** с жёлтой подсветкой совпадений в названии и объекте
- **Скрыть завершённые** (колонка «Завершено»)
- **Обработка действий** (🔔 счётчик заявок требующих действия)
- **Восстановление позиции прокрутки** при возврате «Назад»
- **CSV-экспорт** отфильтрованных заявок

### Карточка заявки (KanbanCard)

```
┌─ [цветная полоска — срочность] ──────────────────────┐
│  [🧱 тип]  #042          [Статус / 🔔 Действие]      │
│  Название заявки (2 строки, поиск-подсветка)          │
│  🏗 Название объекта                                  │
│  [👤 Иванов]          [1.2M сум]  [🔴 КРИТИЧНО]      │
│  [⚠ Просрочено 3д]                        [🕐 5д]    │
├───────────────────────────────────────────────────────│
│  ⏱ SLA: ещё ~18ч  (или ⚠ SLA просрочен на 4ч)       │
│  🔥 Горящее  🧱 Бетон/ЖБ                             │
│  ⎇ Показать цепочку ›                                 │
│  [⚡ Обработать] → [Одобрить] [Отклонить]             │
└───────────────────────────────────────────────────────┘
```

---

## 🏷 ТЕГИ МАТЕРИАЛОВ

| ID | Название | Цвет | Emoji |
|---|---|---|---|
| hot | Горящее | Красный | 🔥 |
| tool | Инструмент | Серый | 🔧 |
| bulk | Сыпучие | Коричневый | 🪨 |
| tech | Техника | Жёлтый | 🏗️ |
| concrete | Бетон/ЖБ | Сине-серый | 🧱 |
| electric | Электрика | Синий | ⚡ |
| plumbing | Сантехника | Голубой | 🚿 |
| finish | Отделка | Фиолетовый | 🎨 |

---

## 🏢 ОБЪЕКТЫ СТРОИТЕЛЬСТВА

```typescript
ConstructionObject:
  id, code, name
  address?
  managerUid? / managerName?   // прикреплённый прораб
  isActive                     // активный / завершённый
  budget?   : number           // общий бюджет
  spent?    : number           // освоено (сумма actualCost заявок)
  createdAt : string
  telegramChatId?              // чат объекта в Telegram
  blocks?   : ObjectBlock[]    // участки и блоки

ObjectBlock:
  id          : string   // уникальный ID
  name        : string   // произвольное название: «Секция А», «Этаж 3», «Кровля»
  description?: string
```

**Блоки** автоматически появляются в форме новой заявки как кнопки-пиллы для поля «Участок». Прораб кликает — поле заполняется.

---

## 📬 TELEGRAM УВЕДОМЛЕНИЯ

### 12 событий

| Событие | Кому | Триггер |
|---|---|---|
| `request_created` | Все | Заявка создана |
| `sklad_needed` | Склад | Заявка передана на склад |
| `nachalnik_needed` | Начальник | Заявка поступила на согласование |
| `nachalnik_approved` | Снабжение | Начальник одобрил |
| `finansist_needed` | Финансист | Заявка поступила финансисту |
| `finansist_approved` | Снабжение | Финансист одобрил |
| `snab_needed` | Снабжение | Готова к закупке |
| `zakupleno` | Склад | Материалы закуплены, нужно выдать |
| `vydano` | Прораб | Заявка выполнена |
| `otkloneno` | Прораб | Заявка отклонена |
| `urgent_created` | Все | Срочная / критичная заявка |
| `low_stock` | Склад | Остаток позиции ≤ minQuantity |

### Конфигурация (TelegramChatConfig)

```typescript
chatId      : string    // ID чата или группы
threadId?   : string    // ID топика (если Forum Group)
events      : TelegramEvent[]  // какие события сюда слать
isActive    : boolean
objectFilter?: string[] // [] = все объекты; [id1,id2] = только эти
mentionTag? : string    // @username для упоминания
```

**Тихий режим:** не отправлять уведомления в заданные часы (UTC+5 Ташкент).

---

## 🏪 СКЛАД

```typescript
StockItem:
  id, name, unit, category
  quantity    : number   // текущий остаток
  minQuantity : number   // минимум для low_stock алерта
  price?      : number   // цена за единицу
  location?   : string   // «Стеллаж А3», «Зона Б»
  updatedAt, updatedBy, updatedByName

StockMovement:
  type        : 'in' | 'out' | 'adjustment'
  quantity    : number
  reason      : string
  requestId?  : string   // привязка к заявке
  requestNumber?: number
```

**Важно:** движения склада создаются вручную кладовщиком. Автосписания при изменении статуса заявки **нет** — это сознательное решение для контроля.

---

## 📊 АНАЛИТИКА

**Фильтр периода:** 7 дней / 30 дней / 90 дней / всё время

**8 KPI-карточек:**
1. Всего заявок за период
2. Выдано (выполнено)
3. Отклонено
4. Общий бюджет (сумма estimatedCost)
5. Срочных заявок (high + critical)
6. Среднее время выполнения (novaya → vydano, по history)
7. Активных объектов
8. Размер команды

**Графики:**
- Разбивка по статусам (горизонтальные бары)
- Разбивка по типам заявок
- Топ объектов по количеству заявок и бюджету
- Нагрузка по исполнителям
- Помесячный тренд (гистограмма)
- Сетка срочности (critical / high / normal / low)

---

## 🗃 КАТАЛОГ МАТЕРИАЛОВ

**96 позиций в 14 категориях** с ценами в сумах:

```
Цемент и смеси     — 11 позиций
Кирпич и блоки     —  6 позиций
Металлопрокат      —  5 позиций
Арматура           —  4 позиции
Дерево и пиломат.  —  9 позиций
Кровля             —  6 позиций
Гидро/теплоизол.   —  6 позиций
Отделочные матер.  — 12 позиций
Электрика          — 13 позиций
Сантехника         — 13 позиций
Крепёж             —  5 позиций
Расходный инстр.   —  8 позиций
СИЗ                —  6 позиций
```

Autocomplete при вводе позиции заявки — автозаполняет `unit`, `category`, `estimatedPrice`.

---

## 🗂 СОХРАНЕНИЕ СОСТОЯНИЯ (UI)

```
sessionStorage (сессия браузера):
  req_viewMode      → kanban / list   (канбан по умолчанию)
  req_quickFilter   → all / mine / need_action / urgent / open / done
  req_search        → текст поиска
  req_scrollY       → позиция прокрутки (восстанавливается при «Назад»)

localStorage (постоянно):
  nirvana_request_draft      → автосохранение черновика новой заявки
  nirvana_request_templates  → сохранённые шаблоны заявок (массив)
```

---

## 🚦 БЫСТРЫЕ ФИЛЬТРЫ СПИСКА / КАНБАНА

| Кнопка | Логика |
|---|---|
| Все | без фильтра |
| Мои действия | `needsMyAction(status, myRole, chain)` |
| Срочные | urgencyLevel = high или critical + не завершена |
| Мои заявки | createdBy = currentUser.uid |
| В работе | status ≠ vydano и ≠ otkloneno |
| Выданные | status = vydano |

**Расширенные фильтры:** статус, тип, срочность, объект, диапазон дат.

---

## ❌ НЕ РЕАЛИЗОВАНО (потенциал для роста)

| Блок | Описание |
|---|---|
| **Дочерние заявки** | При `sklad_partial` — автоматически создаётся дочерняя задача «выдать позиции 1-3», основная идёт дальше в закуп |
| **Gantt / Календарь** | Визуализация `plannedDate` по объектам и ресурсам |
| **Авто-бюджет** | Списание `estimatedCost` из `budget` объекта при одобрении финансистом |
| **Справочник поставщиков** | База поставщиков, история цен, рейтинг надёжности |
| **Счета и платёжки** | Прикрепление скан-документа к статусу `finansist_approved` |
| **Приёмка прорабом** | Кнопка «Подтвердить получение» как финальный шаг от прораба |
| **Telegram SLA-алерты** | Автоотправка в чат при просрочке SLA (сейчас только визуально в UI) |
| **Групповые закупки** | Объединить позиции из разных заявок в один заказ поставщику |
| **Отчёты PDF** | Экспорт карточки заявки / акта выдачи в PDF |
| **Мобильное PWA** | Push-уведомления, оффлайн-режим |
| **Сканер QR** | QR-код на позицию склада → быстрая инвентаризация |
| **Авто-движения склада** | При выдаче заявки автоматически списывать из StockItem |
| **Повторяющиеся заявки** | Шаблон с расписанием (еженедельно, ежемесячно) |
| **Комментарии/чат** | Тредовые комментарии внутри заявки с @упоминаниями |

---

*Обновлено: 24.02.2026 | Проект: Nirvana Luxury Residence | sklad-25dbd.web.app*
