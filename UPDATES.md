# UPDATES — Nirvana Luxury Residence · Система складского учёта

> Все обновления системы в хронологическом порядке.  
> Проект: `sklad-25dbd` · URL: https://sklad-25dbd.web.app  
> Стек: React 18 + TypeScript + Vite + Firebase Firestore + Firebase Hosting + Tailwind CSS

---

## 📦 v1.0 — Базовая система (первоначальный запуск)

### Инфраструктура
- Firebase Firestore (realtime `onSnapshot`) — 6 коллекций: `requests`, `objects`, `users`, `stock`, `movements`, `settings/telegram`
- Firebase Hosting + GitHub Actions CI/CD (auto-deploy при push в `main`)
- Секрет `FIREBASE_SERVICE_ACCOUNT` в GitHub Secrets

### Роли и доступ
| Роль | Описание |
|---|---|
| `prоrab` | Создаёт заявки, видит только свои |
| `sklad` | Выдаёт материалы, управляет складом |
| `nachalnik` | Согласовывает заявки |
| `finansist` | Согласует бюджет |
| `snab` | Закупает материалы |
| `admin` | Полный доступ, управление пользователями |

### Базовый функционал
- Создание заявок с позициями спецификации (`RequestItem`)
- 5 цепочек согласования: `full`, `warehouse_only`, `purchase_only`, `full_finance`, `finance_only`
- 11 статусов воронки от `novaya` до `vydano`/`otkloneno`
- История изменений (`RequestHistoryEntry`) c каждым переходом статуса
- Страницы: Dashboard, Requests, NewRequest, RequestDetail, Warehouse, Objects, Users

---

## 📦 v1.1 — Dashboard и Аналитика

### Изменения
- **DashboardPage**: градиентный заголовок, кликабельный pipeline воронки, панели по ролям (`RoleSpecificPanel` для sklad / nachalnik / finansist / snab / prоrab)
- **AnalyticsPage** (`/analytics`): фильтр периода (7/30/90д/всё), 8 KPI-карточек, графики по статусам / типам / объектам / нагрузке, помесячный тренд

---

## 📦 v1.2 — Профессиональный Канбан (commit `d079fe9`)

### Изменения
- **6 колонок Канбан-доски** с WIP-лимитами и прогресс-барами
- **Swimlane-режим**: объекты × колонки
- Сортировка карточек: срочность / дата / сумма / обновление
- Группировка по объектам
- Сворачивание колонок до 48px полоски
- Компактный режим карточек
- Поиск с жёлтой подсветкой совпадений
- Скрытие завершённых заявок
- CSV-экспорт отфильтрованных заявок

---

## 📦 v1.3 — UX Канбана (commit `630e9f2`)

### Изменения

**Канбан как вид по умолчанию:**
- `viewMode = 'kanban'` при первом открытии
- Сохранение состояния в `sessionStorage`: `req_viewMode`, `req_quickFilter`, `req_search`, `req_scrollY`
- Восстановление позиции прокрутки при возврате «Назад» через `requestAnimationFrame`

**Управление заявками прямо с карточки:**
- Кнопка «⚡ Обработать» разворачивает список доступных статусов
- Смена статуса через `updateDoc` без перехода на страницу заявки
- Сброс `slaEnteredAt` при каждом изменении статуса
- `STATUS_ACTION_STYLE` — цветовая карта кнопок по статусам

**Цепочка с историей:**
- Компонент `ChainTimeline` — горизонтальный ряд шагов с соединительными линиями
- Пройденные шаги — серые с ✓, текущий — синий, будущие — прозрачные
- Метки длительности в днях (`duration` из `history`)
- Разворачивается прямо в карточке Канбана

**Дизайн колонок:**
- Солидные цветные заголовки (не пастельные фоны)
- Белый текст на цветном фоне для чёткого разделения

---

## 📦 v1.4 — Объекты, Теги, SLA (commit `c77fa2d`)

### Блоки объектов (`ObjectBlock`)
- Интерфейс `ObjectBlock { id, name, description? }` в `types.ts`
- В `ConstructionObject`: поле `blocks?: ObjectBlock[]`
- Редактор блоков в модальном окне объекта: добавить / переименовать / удалить
- Блоки отображаются как изумрудные пиллы на карточке объекта
- Модальное окно сделано скроллируемым (`max-h-[90vh]`)

### Зона/участок в новой заявке
- `NewRequestPage`: при выборе объекта автоматически загружаются его блоки
- Поле «Участок» — кнопки-пиллы из блоков объекта + свободный текст ниже
- Выбор блока одним кликом заполняет поле `zone`

### Теги материалов (8 типов)
```
hot      — Горящее    🔥  красный
tool     — Инструмент 🔧  серый
bulk     — Сыпучие    🪨  коричневый
tech     — Техника    🏗️  жёлтый
concrete — Бетон/ЖБ   🧱  сине-серый
electric — Электрика  ⚡  синий
plumbing — Сантехника 🚿  голубой
finish   — Отделка    🎨  фиолетовый
```
- Мультивыбор тегов при создании заявки
- `tags[]` сохраняется в Firestore
- Цветные чипы тегов на Канбан-карточке

### SLA-таймеры (визуальные)
- Константа `SLA_HOURS`: лимиты в часах по каждому статусу
- Расчёт от поля `slaEnteredAt` (сбрасывается при смене статуса)
- Индикатор на Канбан-карточке:
  - ≤75% — серый «SLA: ещё ~Nч»
  - 75–100% — оранжевое предупреждение
  - >100% — красный `ring` + «⚠ SLA просрочен на Nч»
- `slaEnteredAt` устанавливается при создании и при каждой смене статуса

### Канбан — итоговые 6 колонок v1.4
| # | Колонка | Цвет | Статусы |
|---|---|---|---|
| 1 | Новые | `#6b7280` серый | `novaya` |
| 2 | У склада | `#d97706` жёлтый | `sklad_review`, `sklad_partial` |
| 3 | Согласование | `#2563eb` синий | `nachalnik_review`, `nachalnik_approved` |
| 4 | Финансы | `#7c3aed` фиолетовый | `finansist_review`, `finansist_approved` |
| 5 | Закупка | `#0891b2` голубой | `snab_process`, `zakupleno` |
| 6 | Завершено | `#15803d` зелёный | `vydano`, `otkloneno` |

---

## 📦 v2.0 — Четыре ключевых механики (commit `8f1d00d`)

### 2.1 — Сплитование заявок при `sklad_partial`

**Проблема:** кладовщик нажимал «Частично выдать» и заявка просто меняла статус — не было механики разделения.

**Решение:** нажатие «Частичная выдача» **открывает Split-модал** вместо прямой смены статуса.

**Поток:**
```
Кладовщик → «Частичная выдача»
  → Split-модал открывается
  → Для каждой позиции: ввести «Выдать сейчас»
  → Превью: Карточка А (склад) / Карточка Б (закуп)
  → «Разделить и выдать»
      ├── Карточка А (оригинал): статус → vydano, issuedQty проставлены
      └── Карточка Б (дочерняя): статус → nachalnik_review, позиции = остаток
```

**Структура данных:**
```typescript
// На обеих карточках
parentId?   : string    // на Карточке Б: ID родителя
childIds?   : string[]  // на Карточке А: ID дочерних
splitNote?  : string    // комментарий кладовщика
isSplit?    : boolean   // маркер дочерней заявки
```

**ID дочерней заявки:** автоинкремент `max(existing.number) + 1`, заголовок с префиксом `[Закупка]`.

**UI-инфраструктура:**
- Взаимные баннеры-ссылки «Открыть родительскую →» / «Дочерняя заявка →»
- Состояние `splitQty` инициализируется текущими `issuedQty` или `quantity`

---

### 2.2 — Закрытие заявки прорабом

**Проблема:** `vydano` был финальным статусом — прораб не подтверждал факт получения, система не фиксировала реальную приёмку.

**Решение:** добавлены два новых статуса. `vydano` стал промежуточным, `polucheno` — финальным.

**Расширенная воронка:**
```
snab_process → zakupleno → v_puti → vydano → polucheno ✓
                              ↑           ↑
                           снаб        прораб
                         отгрузил    подтвердил
```

**Статусы:**
| Статус | Ответственный | SLA | Описание |
|---|---|---|---|
| `v_puti` | `sklad` | 48 ч | Снаб отгрузил, ждём доставки |
| `vydano` | `prоrab` | 72 ч | Склад выдал — ждём подтверждения |
| `polucheno` | — | — | ФИНАЛЬНЫЙ: прораб подтвердил |

**Поля в Firestore:**
```typescript
prorabConfirmedAt?   : string  // ISO момент подтверждения
prorabConfirmedBy?   : string  // UID прораба
prorabConfirmedByName?: string // Имя прораба
```

**UI:**
- Статус `vydano` показывает зелёный информационный баннер с именем создателя заявки
- Кнопка «✔ Подтвердить приёмку» — крупная, изумрудная, только для роли `prоrab` и только для заявок, которые он сам создал
- Если `prorabConfirmedAt` уже есть — в баннере показывает дату подтверждения

---

### 2.3 — Умное автосписание со склада

**Проблема:** движения склада (`StockMovement`) создавались только вручную — человеческий фактор при выдаче.

**Решение:** при переводе заявки в `vydano` (роль: `sklad`) — автоматический Firestore batch-транзакция.

**Алгоритм `deductStockForIssue(issueMap)`:**
1. Загружает все документы `/stock`
2. Для каждой позиции заявки с `issuedQty > 0` ищет совпадение по `name` (case-insensitive)
3. **Валидация остатка:** если `stock.quantity - issuedQty < 0` → выбросить ошибку с перечнем дефицитных позиций (жёсткий блок, `updateDoc` не вызывается)
4. Если всё ок → `writeBatch`:
   - `stock.quantity -= issuedQty`
   - создаёт `StockMovement { type: 'out', reason: 'Выдано по заявке #N — title', requestId, requestNumber }`
5. Позиции без записи в `/stock` — **пропускаются**, не блокируют

**Пример сообщения об ошибке:**
```
Недостаточно на складе:
«Арматура А12» : нужно 5 т, остаток 2 т
«Цемент М400» : нужно 50 мешков, остаток 30 мешков
```

**Также работает при сплите:** если при Split-модале выдаётся часть — те же позиции списываются до создания дочерней заявки.

---

### 2.4 — SLA-эскалация в Telegram (client-side cron)

**Проблема:** SLA-индикаторы были только визуальными в UI — просроченные заявки могли «висеть» незамеченными.

**Решение:** фоновый клиентский таймер, проверяющий просрочки и форсированно отправляющий Telegram-уведомления.

**Новое событие:** `sla_breached` (добавлено в `TelegramEvent` и `TelegramChatConfig.events[])

**Функция `checkAndNotifySlaBreaches()` в `src/services/telegram.ts`:**
```
Каждые 15 минут (setInterval в App.tsx):
  1. Загружает все незакрытые заявки из /requests
  2. Для каждой: elapsed = now - slaEnteredAt (или updatedAt)
  3. Если elapsed > SLA_HOURS[status] * 3_600_000 → просрочка
  4. Проверяет localStorage: если уже оповещали < 4 часов назад → пропустить
  5. Фильтрует чаты: isActive + events.includes('sla_breached') + objectFilter
  6. Отправляет через Bot API:
     ⏰ ПРОСРОЧКА SLA @mention
     Заявка #N — Название
     Объект: ...
     Этап: У финансиста
     Просрочено на: 14 ч (лимит 24 ч)
     [ссылка на заявку]
  7. Пишет в localStorage: { 'reqId:status': timestamp }
```

**Debounce:** ключ `reqId:status` в localStorage — повторный алерт не чаще **4 часов** на одну и ту же заявку на одном статусе.

**Стартовая задержка:** первый запуск через **30 секунд** после входа (чтобы не спамить при перезагрузке страницы).

**Таблица SLA-лимитов (финальная):**
| Статус | Лимит | Эскалируется |
|---|---|---|
| `novaya` | 48 ч | ✅ |
| `sklad_review` | 24 ч | ✅ |
| `sklad_partial` | 12 ч | ✅ |
| `nachalnik_review` | 24 ч | ✅ |
| `finansist_review` | 24 ч | ✅ (жёсткий) |
| `snab_process` | 72 ч | ✅ |
| `zakupleno` | 24 ч | ✅ |
| `v_puti` | 48 ч | ✅ |
| `vydano` | 72 ч | ✅ |

---

### Изменения в Канбан-доске (v2.0)

**7 колонок** вместо 6:
| # | Колонка | Цвет | Статусы | WIP |
|---|---|---|---|---|
| 1 | Новые | `#6b7280` серый | `novaya` | 10 |
| 2 | У склада | `#d97706` жёлтый | `sklad_review`, `sklad_partial` | 8 |
| 3 | Согласование | `#2563eb` синий | `nachalnik_review`, `nachalnik_approved` | 12 |
| 4 | Финансы | `#7c3aed` фиолетовый | `finansist_review`, `finansist_approved` | 8 |
| 5 | Закупка | `#0891b2` голубой | `snab_process`, `zakupleno`, `v_puti` | 15 |
| 6 | У прораба | `#16a34a` зелёный | `vydano` | ∞ |
| 7 | Закрыто | `#059669` изумрудный | `polucheno`, `otkloneno` | ∞ |

### Новые Telegram-события (v2.0)
| Событие | Триггер |
|---|---|
| `v_puti` | Снаб перевёл в «В пути» |
| `vydano` | Склад выдал прорабу |
| `polucheno` | Прораб подтвердил приёмку |
| `sla_breached` | Автоматическая эскалация при просрочке SLA |

---

## 🗂 Структура файлов проекта

```
src/
├── App.tsx                    — роутинг, SLA cron (setInterval 15 мин)
├── types.ts                   — все TypeScript интерфейсы и типы
├── utils.ts                   — константы, бизнес-логика, каталог материалов
├── firebase.ts                — инициализация Firebase
├── contexts/
│   └── AuthContext.tsx        — текущий пользователь, роль
├── components/
│   └── Layout.tsx             — навигация, шапка
├── pages/
│   ├── DashboardPage.tsx      — дашборд, KPI по ролям
│   ├── RequestsPage.tsx       — список + Канбан (KanbanCard, KanbanBoard, ChainTimeline)
│   ├── NewRequestPage.tsx     — форма создания заявки (блоки, теги, автосохранение)
│   ├── RequestDetailPage.tsx  — детальный просмотр + Split-модал + приёмка прорабом
│   ├── WarehousePage.tsx      — склад: остатки, движения
│   ├── ObjectsPage.tsx        — объекты строительства + блоки/участки
│   ├── AnalyticsPage.tsx      — аналитика: графики, КПЭ
│   ├── UsersPage.tsx          — управление пользователями (admin)
│   ├── TelegramSettingsPage.tsx — настройка Telegram-чатов
│   ├── LoginPage.tsx
│   └── SetupPage.tsx
└── services/
    └── telegram.ts            — Bot API, sendRequestNotification(), checkAndNotifySlaBreaches()
```

---

## 📌 Таблица всех статусов (финальная)

| Статус | Метка | Цвет | Ответственный | SLA |
|---|---|---|---|---|
| `novaya` | Новая | синий | sklad | 48 ч |
| `sklad_review` | У склада | жёлтый | sklad | 24 ч |
| `sklad_partial` | Частично выдано | оранжевый | nachalnik | 12 ч |
| `nachalnik_review` | У начальника | фиолетовый | nachalnik | 24 ч |
| `nachalnik_approved` | Одобрено нач. | индиго | snab | — |
| `finansist_review` | У финансиста | розовый | finansist | 24 ч ⚡ |
| `finansist_approved` | Одобрено фин. | фиолет | snab | — |
| `snab_process` | В снабжении | голубой | snab | 72 ч |
| `zakupleno` | Закуплено | бирюзовый | snab | 24 ч |
| `v_puti` | В пути → | небесный | sklad | 48 ч |
| `vydano` | Выдано ✔ | лайм | prоrab | 72 ч |
| `polucheno` | Получено ✓ | зелёный | — | **ФИНАЛ** |
| `otkloneno` | Отклонено ✗ | красный | — | **ФИНАЛ** |

---

## 🚀 Дорожная карта (нереализованное)

| Приоритет | Функция | Описание |
|---|---|---|
| 🔴 Высокий | **Авто-бюджет** | При одобрении финансиста: `objects.spent += estimatedCost`, предупреждение при достижении 90% бюджета |
| 🔴 Высокий | **Счета и платёжки** | Прикрепление скан-счёта к статусу `finansist_approved`, поле `invoiceUrl` |
| 🟠 Средний | **Групповые закупки** | Объединить позиции из нескольких заявок в один заказ поставщику, агрегация по категориям |
| 🟠 Средний | **Справочник поставщиков** | База поставщиков, история цен, рейтинг надёжности, автозаполнение `preferredSupplier` |
| 🟠 Средний | **Gantt / Календарь** | Визуализация `plannedDate` по объектам и ресурсам, сетка Ганта |
| 🟡 Низкий | **Отчёты PDF** | Экспорт карточки заявки / акта выдачи в PDF с подписями |
| 🟡 Низкий | **Повторяющиеся заявки** | Шаблон с расписанием (еженедельно, ежемесячно), авто-создание |
| 🟡 Низкий | **Комментарии/чат** | Тредовые комментарии внутри заявки с @упоминаниями |
| 🟡 Низкий | **Мобильное PWA** | Push-уведомления (Web Push API), оффлайн-режим |
| 🟡 Низкий | **Сканер QR** | QR-код на позицию склада → быстрая инвентаризация с камеры |

---

## 📋 История коммитов

| Коммит | Описание |
|---|---|
| начало | Firebase setup, базовая система |
| — | Dashboard redesign, роли, навигация |
| — | GitHub Actions CI/CD |
| — | AnalyticsPage, CSV-экспорт |
| `d079fe9` | Профессиональный Канбан (swimlane, WIP, сортировка, компакт) |
| `630e9f2` | Inline-управление, ChainTimeline, scroll restore, sessionStorage |
| `c77fa2d` | ObjectBlocks, MATERIAL_TAGS, SLA-таймеры, 6 колонок, zone picker |
| `8f1d00d` | Split-модал, автосписание склада, приёмка прорабом, v_puti/polucheno, SLA cron |

---

*Последнее обновление: 24.02.2026 · sklad-25dbd.web.app*
